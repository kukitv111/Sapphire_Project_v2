route:
  receiver: local-alert-log
  group_by: [alertname, instance, service_name]
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: local-alert-log
    webhook_configs:
      - url: http://alert-log:8080/
        send_resolved: true
